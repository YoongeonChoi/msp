{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability without broker or shell access",
  "windows": ["main"],
  "permissions": ["core:default"]
}

