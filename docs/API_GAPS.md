# API Gaps

2026-07-14에 취득한 Toss source index와 OpenAPI 원본 바이트의 URL·취득 시각·
SHA-256은 [Provider Contract Artifacts](PROVIDER_CONTRACT_ARTIFACTS.md)에 고정한다.
해당 증거는 로컬 `contract_test` 검증용이며 sandbox 또는 외부 주문 승인이 아니다.

| Provider | Gap | Status | Exact verification step |
| --- | --- | --- | --- |
| Toss | Auth token issue/refresh flow | verified-readonly-implemented | Official OpenAPI JSON confirms `POST /oauth2/token`, OAuth2 Client Credentials Grant, form fields `grant_type`, `client_id`, `client_secret`, and no refresh token |
| Toss | account/position query schema | verified-readonly-implemented | Official OpenAPI JSON confirms `GET /api/v1/accounts`, `GET /api/v1/holdings`, and `X-Tossinvest-Account=accountSeq` |
| Toss | cash buying power query | verified-readonly-implemented | Official OpenAPI JSON confirms `GET /api/v1/buying-power?currency=KRW` with `cashBuyingPower` decimal string |
| Toss | quote/current price query | verified-readonly-implemented | Official OpenAPI JSON confirms `GET /api/v1/prices?symbols=...` result fields `symbol`, `timestamp`, `lastPrice`, `currency` |
| Toss | KR market calendar | verified-readonly-implemented | Official OpenAPI JSON confirms `GET /api/v1/market-calendar/KR` with `today.integrated.regularMarket.startTime`, optional `singlePriceAuctionStartTime`, and `endTime` |
| Toss | candle/history query | verified-readonly-implemented | Official OpenAPI JSON confirms `GET /api/v1/candles` params `symbol`, `interval`, `count`, `before`, `adjusted` and `CandlePageResponse`; the adapter additionally requires identity encoding, a bounded duplicate-free JSON document, exact envelope/page/item fields, and exact request/domain types before canonical point-in-time conversion |
| Toss | historical candle completeness, authenticity, and finality evidence | unverified-blocking-research-certification | Obtain a provider-issued, versioned artifact that defines inclusive-range completeness, correction and adjusted-price semantics, finalization timing, artifact identity, and verification method. Retain its exact bytes, retrieval time, source URI, and SHA-256, then implement an independently reviewed verifier receipt before adding a positive research-certification path |
| Toss | production order status/history | verified-contract-quarantined | Official OpenAPI JSON documents `GET /api/v1/orders` and `GET /api/v1/orders/{orderId}`, but the G1+G2 `NO-LIVE` boundary requires zero production order URL calls. Status/reconciliation qualification therefore runs only against local `contract_test`; the Toss adapter fails before network I/O |
| Toss | production order create execution | verified-contract-quarantined | The official contract documents `POST /api/v1/orders`, but this release MUST NOT call it. Startup rejects production order endpoints and order-capable credentials; create lifecycle qualification runs only against the local `contract_test` simulator. Reopening this boundary requires a new G0/regulatory review and separately approved provider contract evidence |
| Toss | production order cancel/modify execution | verified-contract-quarantined | The official contract documents cancel/modify operations, but this release MUST NOT call either endpoint. Cancel lifecycle qualification runs only against the local `contract_test` simulator; modify is outside G1+G2 scope |
| Toss | official sandbox environment | unverified-blocking-external-writes | The public source of truth identifies only `https://openapi.tossinvest.com`. Obtain a provider-issued sandbox base URL, credential scope, account isolation guarantee, rate/error contract, and retained artifact SHA-256 before adding any external write adapter. The local simulator must be labelled `contract_test`, never sandbox |
| Toss | account/market observation for scheduled cycle | readonly-only | Official account, buying-power, holdings, quote, candle, and calendar reads may be used for observation. Production order URLs are excluded. Read data never authorizes an order write or strategy promotion |
| KRX | market calendar endpoint | verified-not-implemented-replaced-by-toss | KRX Open API market-calendar product is not implemented or used in the live/non-mock worker path; Korean market-open gating is supplied by the already verified Toss `GET /api/v1/market-calendar/KR` contract, and `HealthService` now records live market-data health as `toss_market_data` instead of mislabeling it as `krx` |
| KRX | official historical calendar completeness evidence | unverified-blocking-research-certification | Obtain a KRX-issued, versioned historical calendar artifact or documented product covering the exact inclusive date range, corrections, finalization semantics, retrieval time, and stable artifact identity. Retain and independently verify the exact bytes and SHA-256 before any local retained coverage can be promoted to official-exchange completeness |
| KRX | listing/sector statistics | verified-not-live-critical | KRX listing/sector APIs remain outside the live trading safety path; the current worker does not depend on KRX listing/sector data for order creation, and any direct `KrxClient` usage remains fail-closed until exact product approval, endpoint, params, and schema are verified |
| OpenDART | corp code mapping | verified-readonly-implemented | Official OpenDART guide confirms `GET /api/corpCode.xml?crtfc_key=...` returns a ZIP containing `CORPCODE.xml` with `corp_code`, `corp_name`, `stock_code`, and `modify_date`; worker parses the official XML shape with `defusedxml`, maps Korean stock code to `corp_code`, rejects oversized ZIP/XML and DTD/entity declarations, and fails malformed ZIP/XML closed |
| OpenDART | account name mapping | verified-partial-fundamentals-implemented | Official OpenDART financial statement guide confirms `GET /api/fnlttSinglAcntAll.json` with `corp_code`, `bsns_year`, `reprt_code`, and `fs_div`, returning rows with `account_nm` and current amount fields; worker maps Korean account names for revenue, operating income, net income, assets, liabilities, and equity to compute `operating_margin`, `roe`, and `debt_ratio`. `per`/`pbr` remain `None` because they require market price/capitalization data outside OpenDART financial statements |
| Naver | rate limits and error schema | verified-readonly-implemented | Official Naver Search News API docs confirm `GET https://openapi.naver.com/v1/search/news.json`, required `X-Naver-Client-Id` and `X-Naver-Client-Secret` headers, `query`/`display`/`start`/`sort` params, response item fields, documented daily call limit, and error envelopes with `errorCode`/`errorMessage`; worker maps auth, rate-limit, schema, timeout, unavailable, and unknown failures to fail-closed provider errors |
| OpenAI | model/version choice for structured outputs | verified-structured-output-guarded | Official OpenAI docs confirm Responses API structured outputs through `text.format.type=json_schema` with `strict=true` and model support for current GPT-5/GPT-4.1/GPT-4o families; worker default is `gpt-5.5`, and any unverified `OPENAI_MODEL` fails closed with `openai_structured_output_model_not_verified` before an API call |
| Supabase | Free plan database limit | verified-budget-enforced | Official Supabase plan docs confirm Free project database space is 500MB; MVP keeps an internal 500MB budget and warns at `PAPER_HEALTH_DB_WARNING_BYTES=450000000` before the Free cap |

## 연구 데이터 전용 공백

KR corporate-action coverage와 adjustment evidence의 공식 source는 아직 선택되지
않았다. Positive corporate-action 또는 full-DQ 인증을 추가하기 전에 공식 source를
review로 확정하고, event identity/type, ex/effective/record date, split 또는 adjustment
factor, 명시적 empty-range coverage, correction과 finality를 포함하는 versioned contract와
exact artifact를 보존해야 한다. Retrieval URI/time, exact bytes, SHA-256과 독립 검증 receipt가
모두 필요하다. 이 연구 데이터 공백은 위 Live provider-gap 표의 행이나 통과 상태로
해석하지 않는다.
